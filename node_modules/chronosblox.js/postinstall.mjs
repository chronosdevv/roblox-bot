import { Chalk } from "chalk";
import figlet from "figlet";

const chalk = new Chalk();
console.log(
  chalk.yellow(
    figlet.textSync("chronosblox.js", {
      font: "Big",
      horizontalLayout: "default",
      verticalLayout: "default",
    })
  )
);

console.log(`
${chalk.underline("Thank you for installing chronosblox.js.")}`);
